# RSS News Fetcher Requirements
# Minimal dependencies for RSS-based news extraction

# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0

# RSS feed parsing (NEW - replaces chardet for scraping)
feedparser>=6.0.10

# NLP/LLM
groq>=0.4.0

# Optional but recommended
python-dateutil>=2.8.0  # Better date parsing for RSS feeds

# If you still need web scraping fallback
chardet>=5.2.0  # Only needed if falling back to HTML scraping

# For data processing
pandas>=2.0.0  # Optional: for CSV exports, data analysis
